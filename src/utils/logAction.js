const mark_read = 'mark read';
const mark_unread = 'mark unread';
const mark_new = 'mark new';
const mark_existing = 'mark existing';
const mark_completed = 'mark completed';
const mark_incomplete = 'mark incomplete';
const select_labels = 'select labels';
const update_client = 'update client';

export default {
  mark_read,
  mark_unread,
  mark_new,
  mark_existing,
  mark_completed,
  mark_incomplete,
  select_labels,
  update_client
};
